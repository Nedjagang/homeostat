# ClaimPilot + eval spine
openlit
opentelemetry-api
opentelemetry-sdk
openai
fastapi
uvicorn
python-dotenv
# SSL verification against the OS trust store (internal https OTLP endpoints whose
# CA chain isn't in certifi)
truststore
# RAG / agent
langgraph
langchain
langchain-openai
# Tier-1 cheap proxy (embeddings / local NLI) — optional, not wired yet
# sentence-transformers
