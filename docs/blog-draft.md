# Blog draft (Dev.to / Medium / Substack — a judged deliverable)

> AI-slop is an explicit disqualifier. Postmortem voice, first person, real screenshots,
> measured numbers. Never a result without a screenshot.

## Outline
1. The story — green dashboards, a confidently wrong claim decision.
2. What we built — faithfulness as a golden signal (SLI) in SigNoz.
3. How SigNoz caught + healed it — screenshots, real service names, exact commands, the
   **measured** cost numbers (off the span) and the judge **calibration** numbers.
4. One honest "what genuinely broke" section (from `what-broke/`).
5. Future work — noise report / false-positive tuning, more failure classes.
6. Disclosures — warm-up prototype (clean-room), AI-assistant use.

Cite SigNoz issues #8865 (LLM observability) and #1590 (readable trace JSON).
